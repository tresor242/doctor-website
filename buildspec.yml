version: 0.2

phases:
  install:
    commands:
      - echo "Nothing to install for static site"
  build:
    commands:
      - echo "No build step needed for static HTML/CSS"
  post_build:
    commands:
      - echo "Uploading static site to S3..."
      - aws s3 sync . s3://doctor-website --delete

artifacts:
  files:
    - '**/*'
  base-directory: .
